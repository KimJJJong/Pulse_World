Use the `unity-synaptic` MCP server for Unity Editor, scene, prefab, GameObject, component, and play-mode tasks in this workspace.

For this server, start with `list_categories`, then `list_tools(category)`, then `execute(tool_name, params)`.
